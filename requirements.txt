# Reconstructed from the project's imports and settings -- pin to the versions
# you actually deploy with once confirmed.
Django>=4.1,<5.0
channels>=4.0
channels-redis>=4.0
daphne>=4.0
django-allauth>=0.56          # 0.56+ is required for allauth.account.middleware.AccountMiddleware
django-cors-headers>=4.0
djangorestframework>=3.14
psycopg2-binary>=2.9
python-dotenv>=1.0            # optional: lets settings.py read a local .env file
